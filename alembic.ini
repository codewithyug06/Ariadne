# Copyright 2026 The Ariadne Authors
# SPDX-License-Identifier: Apache-2.0
#
# The database URL is injected from ariadne.config.Settings by env.py, so it is
# deliberately absent here — no credentials live in this file.

[alembic]
script_location = ariadne/db/migrations
prepend_sys_path = .
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s
# `timezone` is deliberately unset: it requires tzdata, which is absent from
# python:*-slim images and from stock Windows, and it only affects filenames.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
